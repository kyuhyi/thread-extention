# 웹툰 이미지가 들어간 블로그 포스팅

## 시작

1. 필요한 Python 패키지 설치: `python -m pip install -r requirements.txt`
2. `codex login status`가 ChatGPT 로그인 상태인지 확인합니다. 필요한 경우 `codex login`을 실행합니다.
3. `start_server.bat`을 실행합니다. 기존 서버가 실행 중이면 종료하고 다시 실행합니다.
4. Chrome 또는 Whale의 확장 관리에서 이 확장 프로그램을 새로고침합니다.

## 사용

- 콘텐츠를 생성하면 블로그 본문에 맞는 웹툰 5장이 순서대로 만들어집니다. 블로그 탭에서 4장을 선택할 수도 있습니다.
- 원하면 ‘웹툰 자동 생성’을 끄고 ‘웹툰 이미지 생성’을 눌러 시작합니다.
- 생성된 글을 직접 넣으려면 [큰 화면 편집기](http://localhost:5000/blog-editor.html)의 ‘본문 직접 입력 / 수정’을 사용합니다. 이 화면은 확장 프로그램의 큰 화면과 저장소가 분리되어 있습니다.
- 팝업을 닫아도 서버에서 이미지 생성이 계속됩니다. 서버가 중단되면 다시 실행하고 실패한 이미지 재시도를 누릅니다.
- 모든 이미지가 준비되면 ‘글+이미지 복사’를 누르고 네이버 글쓰기 본문에서 Ctrl+V로 붙여넣습니다. 업데이트된 확장 프로그램이 켜져 있어야 문단과 사진을 순서대로 자동 첨부합니다. Ctrl+Shift+V는 글자만 붙여넣기이므로 사용하지 않습니다.
- 붙여넣는 동안 네이버 화면 오른쪽 아래 진행 안내가 표시됩니다. 완료될 때까지 편집기를 클릭하지 마세요. 이미지 첨부 완료 후 내용을 확인하고 저장합니다. 자동 발행은 하지 않습니다.
- 복사용 이미지는 5MB 제한보다 충분히 작게 최적화합니다. 전체 복사 데이터가 4MB를 넘으면 복사 전에 차단합니다.
- ‘HTML 저장’은 이미지가 포함된 문서를 저장합니다. ‘이미지 원본 ZIP’에는 PNG 파일들과 본문 텍스트가 들어 있습니다.

## 스레드 글 형식

- 첫 줄은 숫자를 포함하며, 공백·문장부호까지 포함해 최대 18자입니다. 생성 시 15~18자를 목표로 합니다.
- 첫 줄 다음에 빈 줄을 정확히 한 줄 두고 짧은 본문을 이어갑니다.
- 본문은 2~4개의 짧은 문장, 최대 180자로 요청합니다.
- 생성 결과가 조건을 벗어나면 첫 줄을 ‘1분 안에 읽는 핵심 이야기’로 보정하고 원래 내용은 본문에 보존합니다.
- 화면 표시와 복사 결과에 동일한 형식을 적용합니다.

## 저장 및 사용량 상세

- Codex 계정의 포함 사용량이 적용됩니다. 이미지 생성은 일반 텍스트보다 사용량을 더 소모할 수 있습니다.
- 글 생성은 기존 Gemini 설정을 사용합니다. 웹툰 이미지 생성은 로컬 Codex의 ChatGPT 로그인을 사용합니다.
- OAuth 토큰이나 API 키를 확장 프로그램으로 복사하지 않습니다.
- 작업 파일은 `output/blog-images/<작업ID>/`에 저장됩니다. 기존 결과는 자동으로 삭제하지 않습니다.
- 미리보기·복사·HTML 저장에는 블로그 표시 크기에 맞춘 JPEG를 사용하고, ZIP에는 원본 PNG를 보관합니다.
- 기본적으로 한 번에 한 이미지씩 생성하며 장면별 최대 대기 시간은 10분입니다.
- 네이버는 일반 HTML의 data 이미지를 제외할 수 있으므로 전용 붙여넣기 처리를 사용합니다. 확장 프로그램을 다시 로드한 후 네이버 글쓰기 페이지도 새로 열어주세요.

## 검증 결과

2026-09-10 확인 결과:

- 자동 테스트 20개 통과. 스레드 형식, 이미지 순서, 복사 용량 제한, 파일 경로, 작업 복원, 재시도, 확장 로드 리소스 검사 포함.
- 실제 Codex 로그인으로 내용에 맞는 웹툰 5장 생성 완료. 서버 재시작 후 완료 작업 복원 및 PNG 5장 ZIP 검증 완료.
- 복사 데이터는 예시 글 기준 약 15MB에서 0.59MB로 감소. 네이버 5MB 용량 오류가 재현되지 않음.
- 네이버 일반 HTML 붙여넣기는 data 이미지를 제외함. 사진 클립보드로 전달한 이미지 1장의 실제 네이버 업로드 확인.
- 네이버 전용 문단·사진 순차 삽입 코드는 추가했지만, 확장 프로그램 재로드 후의 5장 자동 삽입·임시저장 검증은 아직 남아 있음.
- 확장 로드를 막던 __pycache__ 폴더 3개를 로컬 앱 데이터의 BlogWebtoon/cache-backup으로 이동. 실행 배치와 테스트에서 -B를 사용하며, 테스트 후 캐시가 0개임을 확인.
# 네이버 붙여넣기 업데이트 (1.3.1)

1. Chrome 확장 관리에서 이 폴더의 확장을 새로고침하고, 열어둔 큰 화면과 네이버 글쓰기 페이지도 새로고침합니다.
2. 네이버 글쓰기 페이지를 열어둔 상태에서 이미지 4~5장이 모두 준비되면 `글+이미지 복사`를 누릅니다. 네이버 연결이 확인돼야 복사가 진행됩니다.
3. 네이버 **본문**을 클릭하고 Ctrl+V를 한 번 누릅니다. 문단과 JPEG 사진이 순서대로 들어가며, 완료 안내가 뜰 때까지 편집기를 클릭하지 않습니다.
4. 본문은 왼쪽 정렬, 짧은 문단과 빈 줄로 삽입됩니다. 제목 칸은 따로 입력하고 내용을 확인한 뒤 저장합니다.

2026-09-10 검증: 시험용 네이버 편집기에 수신기 처리 코드를 임시 적용하여 5장 한 번 붙여넣기, 임시저장, 새로 열기 후 원격 사진 5장과 왼쪽 정렬 보존을 확인했습니다. 시험 글은 발행하지 않았습니다. 설치된 확장은 새로고침 후 업데이트가 적용됩니다.
